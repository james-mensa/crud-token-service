import { Container, Theme } from "@mui/material"


export const PageBase=()=>{
    return(
        <Container>

        </Container>

    )
}
const styles={
      wrapper: (theme: Theme) => ({
        backgroundColor: "white",
        minHeight: 200,
        borderRadius: 2,
        padding: 2,
      }),
}